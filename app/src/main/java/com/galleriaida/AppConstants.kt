package com.galleriaida

/**
 * App-wide constants.
 * Change DEV_PLAYER_NAME here to update all dev/bypass checks across the app.
 */
object AppConstants {
    /** Developer/test player name that bypasses star costs and other restrictions. */
    const val DEV_PLAYER_NAME = "George S."
}
